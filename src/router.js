import VueRouter from 'vue-router'
import { removeDotSegments } from '_uri-js@4.2.2@uri-js'
// 3. 创建路由对象
var router = new VueRouter({
  routes: [
    
  ],
  linkActiveClass: 'mui-active'
})

// 把路由对象暴露出去
export default router